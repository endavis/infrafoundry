---
# Kubernetes Inventory - Uses localhost with kubectl
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ '{{' }} ansible_playbook_python {{ '}}' }}"
  vars:
    kubeconfig: "{{ kubeconfig_path | default('~/.kube/config') }}"
